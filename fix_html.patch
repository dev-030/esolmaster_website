--- src/webcomponents/sameroute/class/tasks/question_type/MatchingQuestion.tsx
+++ src/webcomponents/sameroute/class/tasks/question_type/MatchingQuestion.tsx
@@ -172,9 +172,10 @@
   return (
     <div className="space-y-4">
       {/* Question */}
-      <p className="text-base font-medium leading-relaxed text-foreground">
-        {question.config.question || "Match the following items."}
-      </p>
+      <div 
+        className="text-base font-medium leading-relaxed text-foreground prose prose-slate max-w-none prose-p:my-0"
+        dangerouslySetInnerHTML={{ __html: question.config.question || "Match the following items." }}
+      />
 
       {!submitted && (
         <p className="text-sm text-muted-foreground mb-4">
--- src/webcomponents/sameroute/class/tasks/question_type/TextInputQuestion.tsx
+++ src/webcomponents/sameroute/class/tasks/question_type/TextInputQuestion.tsx
@@ -14,9 +14,10 @@
   return (
     <div className="space-y-4">
       {/* Question */}
-      <p className="text-base font-medium leading-relaxed text-foreground">
-        {question.config.question}
-      </p>
+      <div 
+        className="text-base font-medium leading-relaxed text-foreground prose prose-slate max-w-none prose-p:my-0"
+        dangerouslySetInnerHTML={{ __html: question.config.question || "" }}
+      />
 
       <div className="space-y-2">
         <Input
--- src/webcomponents/sameroute/class/tasks/question_type/OrderingQuestion.tsx
+++ src/webcomponents/sameroute/class/tasks/question_type/OrderingQuestion.tsx
@@ -165,9 +165,10 @@
   return (
     <div className="space-y-4">
       {/* Question / Instructions */}
-      <p className="text-base font-medium leading-relaxed text-foreground">
-        {question.config.question || "Drag the items into the correct order."}
-      </p>
+      <div 
+        className="text-base font-medium leading-relaxed text-foreground prose prose-slate max-w-none prose-p:my-0"
+        dangerouslySetInnerHTML={{ __html: question.config.question || "Drag the items into the correct order." }}
+      />
 
       <DragDropContext onDragEnd={handleDragEnd}>
         <Droppable droppableId={`ordering-${question.id}`}>
--- src/webcomponents/sameroute/class/tasks/question_type/GapMCQQuestion.tsx
+++ src/webcomponents/sameroute/class/tasks/question_type/GapMCQQuestion.tsx
@@ -13,7 +13,8 @@
 
   const selected = userAnswer;
 
-  const parts = question.config.question.split("__");
+  const rawQuestion = (question.config.question || "").replace(/<[^>]*>?/gm, '');
+  const parts = rawQuestion.split("__");
 
   return (
     <div className="space-y-4">
--- src/webcomponents/sameroute/class/tasks/question_type/WordBoxMatchQuestion.tsx
+++ src/webcomponents/sameroute/class/tasks/question_type/WordBoxMatchQuestion.tsx
@@ -95,7 +95,8 @@
       {/* Sentences with Drag Targets */}
       <div className="space-y-4">
         {normalizedSentences.map((sentence) => {
-          const parts = sentence.text.split("__");
+          const rawText = (sentence.text || "").replace(/<[^>]*>?/gm, '');
+          const parts = rawText.split("__");
           const isAnswered = !!userAnswer?.[sentence.key];
 
           return (
