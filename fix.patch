--- src/webcomponents/teacher/assign-task/TaskPreviewModal.tsx
+++ src/webcomponents/teacher/assign-task/TaskPreviewModal.tsx
@@ -32,7 +32,15 @@
   const [isCompleted, setIsCompleted] = useState(false);
 
   const meta = TASK_TYPE_META[taskData.type.toLowerCase()] || { label: taskData.type, emoji: "📝", variant: "info" };
-  const questions = taskData.questions || [];
+  const questions = (taskData.questions || []).map((q, index) => ({
+    id: q.id || `preview-${index}`,
+    type: q.type,
+    order: index + 1,
+    config: {
+      question: q.content,
+      ...q.config
+    }
+  }));
   const currentQuestion = questions[currentIndex];
   const totalQuestions = questions.length;
   
