--- src/webcomponents/teacher/assign-task/ActivityBuilder.tsx
+++ src/webcomponents/teacher/assign-task/ActivityBuilder.tsx
@@ -493,6 +493,15 @@
           </Card>
         </div>
       </div>
+      <TaskPreviewModal 
+        open={isPreviewOpen} 
+        onOpenChange={setIsPreviewOpen} 
+        taskData={{
+          title,
+          type: taskType,
+          questions,
+          readingContent: taskType === "READING" && readingPassage ? { text: readingPassage } : undefined,
+        }}
+      />
     </div>
   );
 }
